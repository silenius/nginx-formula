{% import_yaml "defaults/nginx.yaml as defaults %}
{% import_yaml "defaults/os.yaml" as os %}

{% set nginx = salt.grains.filter_by(
    defaults,
    merge = salt.grains.filter_by(
        os, grain='os_family'
    )
    base='nginx'
) %}
